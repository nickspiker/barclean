//! barclean desktop host.
//!
//! Runs the same `FluorApp` implementation the Android build uses, over fluor's
//! winit/softbuffer shell. Desktop exists so the decoder can be worked on
//! against the corpus at compiler speed rather than adb speed.

fn main() {
    eprintln!("barclean desktop UI: not yet implemented (phase 1)");
    std::process::exit(1);
}
